package com.test.us.functional;

import org.openqa.selenium.support.PageFactory;
import org.testng.Assert;
import org.testng.annotations.Test;

import com.toy.pages.HomePage;
import com.toy.selenium.core.BaseTest;
import com.toy.utilities.Utilities;

public class GeoLocated extends BaseTest{
	
	@Test
	public void zipcodeGeoLocatedTest() throws Exception{
		/*
		 * homePage = PageFactory.initElements(getWebDriver(), HomePage.class);
		 * Thread.sleep(2000); System.out.println(homePage.geolocatedZipCode());
		 * 
		 * int expectedZipCode =
		 * Utilities.getNumberFromString(homePage.geolocatedZipCode());
		 * System.out.println(expectedZipCode); reportLog("submit zip code");
		 * //Thread.sleep(12000);
		 * 
		 * homePage.clickOnZipCodePencil(); int actualZipCode =
		 * Integer.parseInt(homePage.getZipCodeFromPopUp());
		 * System.out.println(actualZipCode);
		 * 
		 * Assert.assertEquals(actualZipCode, expectedZipCode);
		 */
		
		
	}

}
